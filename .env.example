# Signing identity for the macOS binary. Without it the build is ad-hoc signed and macOS drops
# Input Monitoring on every rebuild. List identities with:
#   security find-identity -v -p codesigning
# The file is sourced by the shell, so quote values containing spaces.
CHATTER_CODESIGN_IDENTITY="Developer ID Application: Your Name (TEAMID)"
